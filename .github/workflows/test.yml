name: Unit Tests

on:
  push:
    branches: [dev]
  pull_request:
    branches: [ "dev" ]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Running Unit Tests
        run: dotnet test
